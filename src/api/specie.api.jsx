import { config, configApiBackend } from '../config';
export class Species {
  async getAllspecies() {
    try {
      // const url = `${ENV.BASE_API}/${}`
    } catch (error) {
      throw error;
    }
  }
}
